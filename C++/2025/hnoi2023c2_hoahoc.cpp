#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

#define faster ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
#define read freopen("HOAHOC.INP", "r", stdin);
#define write freopen("HOAHOC.OUT", "w", stdout);
const ll N = 1e5 + 5;

void Solve() {
	int a, b;
	cin >> a >> b;
	
	cout << min(a / 3, b / 2);
}

int main() {
	//read
	//write
    faster
    
    Solve();
    
    return 0;
}